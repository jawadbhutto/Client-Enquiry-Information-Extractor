# Client-Enquiry-Information-Extractor
A simple Python application that uses an LLM API to extract structured information from client enquiries into JSON. It identifies missing details, uses a .env file for secure API keys, and was built during my first week of internship.
